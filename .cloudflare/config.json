{
  "build": {
    "command": "npm install && npm run build",
    "output": ".next",
    "environment": {
      "NODE_VERSION": "18",
      "NPM_VERSION": "9",
      "NODE_ENV": "production"
    }
  }
}
